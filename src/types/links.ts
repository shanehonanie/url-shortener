export interface LinkRecordInput {
  user: string;
  shortURL: string;
  longURL: string;
  clicks: number;
}

export interface LinkRecord {
  user: string;
  shortURL: string;
  longURL: string;
  clicks: number;
}
